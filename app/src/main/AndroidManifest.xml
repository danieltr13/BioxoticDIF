<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="trinidad.daniel.bioxotic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bioxotic">
        <activity android:name=".Request_code"></activity>
        <activity android:name=".Request_email" />
        <activity android:name=".forgot_password" />
        <activity android:name=".Hospital_contacts" />
        <activity android:name=".Animal_description" />
        <activity android:name=".Menu_animal_species" />
        <activity android:name=".Menu_species" />
        <activity android:name=".SignUp" />
        <activity android:name=".SingIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Splash_screen" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>